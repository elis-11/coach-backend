<%- include('_header.ejs')
	%>
	<h2>
		<%=pageTitle%>
	</h2>
	<p>This page displays all the speakers at the conference.</p>
	<ul>
		<% sponsors.forEach(sponsor => { %>
			<li><%=sponsor.name %> from  <%=sponsors.location %>.</li>
		<% }) %>
	</ul>
	<%- include('_footer.ejs')
		%>

		